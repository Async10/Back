
1

begin dup 100 <= while
    dup 3 % 0 == dup if
        'F' emit
        'i' emit
        'z' emit
        'z' emit
    end

    swap

    dup 5 % 0 == dup if
        'B' emit
        'u' emit
        'z' emit
        'z' emit
    end

    rot

    + 0 == if
        dup .
    else
        '\n' emit
    end

    1 +
end